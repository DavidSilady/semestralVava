package controller;

import controller.abstracts.Controller;
import controller.interfaces.ListablePaneController;
import model.interfaces.Listable;

public class VideoListingController extends Controller implements ListablePaneController {
    @Override
    public void init(Listable listing, ListingContainerController parentContainer) {

    }
}
